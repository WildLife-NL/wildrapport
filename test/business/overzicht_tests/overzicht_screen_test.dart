import 'package:flutter_test/flutter_test.dart';

void main() {
  group('OverzichtScreen (legacy)', () {
    test(
      'legacy overzicht screen tests are skipped (screen removed)',
      () {},
      skip: 'OverzichtScreen bestaat niet meer in de huidige flow.',
    );
  });
}
